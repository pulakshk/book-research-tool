# Agent Notes

Use `directives/AGENTS.md` as the canonical repo-wide operating guide.

This local note exists only to keep the skill self-contained:

- Route with `OUTLINE.md`
- Pick one module skill
- Prefer active masters and outputs over `_archive/`
- Treat `.claude/worktrees/` and `.tmp/` as noise unless explicitly needed
